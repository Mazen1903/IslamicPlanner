export const font = (params: any) => ({ type: 'font', params });
export const foregroundStyle = (style: any) => ({ type: 'foregroundStyle', style });
export const padding = (params?: any) => ({ type: 'padding', params });
export const frame = (params: any) => ({ type: 'frame', params });
export const lineLimit = (limit?: any) => ({ type: 'lineLimit', limit });
